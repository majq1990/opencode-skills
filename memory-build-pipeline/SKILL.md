---
name: memory-build-pipeline
description: [memory] Personal memory: build-pipeline
---

<!-- Auto-generated by skills-manager memory module.
     Source: C:\Users\majq1\.agent-memory\build-pipeline.md
     Do not edit here — edit the source and run `skills-manager-cli memory sync`. -->

**Memory type**: unspecified

# Build Pipeline Memory

## 钉钉表格集成（2026-06-05 新增）

### 功能说明
打包完成后自动将记录写入钉钉在线表格，便于追踪和管理。

### 表格地址
https://alidocs.dingtalk.com/i/nodes/2Amq4vjg89gq7LzDsPB9boynV3kdP0wQ

### 表格字段
| 字段 | 说明 |
|---|---|
| 软件名称 | 打包的软件名（如 openresty、redis） |
| 版本 | 软件版本号 |
| 操作系统 | OS 家族（centos、ubuntu、openeuler、anolis、kylin、uos） |
| 系统版本 | OS 版本号（如 7、22.04、24.03-lts） |
| 下载路径 | 完整的 HTTP 下载 URL |
| 安装依赖清单 | 运行时依赖（从 meta.yaml 的 runtime_depends 读取） |

### 实现细节
- **数据收集**：`controller/dingtalk.py` 的 `collect_build_records()` 函数
- **格式化**：`format_records_for_dingtalk()` 函数
- **依赖信息**：从 `recipes/<software>/<version>/meta.yaml` 的 `runtime_depends` 字段读取
- **写入方式**：通过 `dws` CLI 调用钉钉表格 API (`dws spreadsheet update-range`)
- **写入时机**：在打包完成、产物上传成功后自动执行（pack.py 步骤 9）

### 关键文件
- `controller/dingtalk.py` - 钉钉表格写入模块
- `pack.py` - 主入口，步骤 9 调用钉钉表格写入
- `SKILL.md` - 已更新，包含钉钉表格记录说明

### 钉钉表格配置
- **nodeId**: `2Amq4vjg89gq7LzDsPB9boynV3kdP0wQ`
- **sheetId**: `kgqie6hm` (Sheet1)
- **表头**: A1:F1 = [软件名称, 版本, 操作系统, 系统版本, 下载路径, 安装依赖清单]

### 技术栈
- Python 3.9+
- PyYAML（读取 meta.yaml）
- dws CLI v1.0.7（调用钉钉表格 API）

### 测试验证
- ✅ PyYAML 已安装
- ✅ dws CLI v1.0.7 可用
- ✅ dingtalk.py 语法检查通过
- ✅ pack.py 语法检查通过
- ✅ collect_build_records() 功能测试通过

### 目录结构说明
- `D:\opencode\config\skills\build-pipeline\SKILL.md` 是 `D:\git\opencode-skills\build-pipeline\SKILL.md` 的 hardlink
- 修改任一路径的文件都会自动同步到另一个路径
