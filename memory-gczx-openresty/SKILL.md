---
name: memory-gczx-openresty
description: [memory] gczx.egova.com.cn 切 openresty + 换域名进度，变更①完成待做变更②换引擎
---

<!-- Auto-generated by skills-manager memory module.
     Source: C:\Users\majq1\.agent-memory\project_gczx_openresty_migration.md
     Do not edit here — edit the source and run `skills-manager-cli memory sync`. -->

**Memory type**: project

会话「登录gczx + 参考demo配openresty」。目标：gczx 从系统 nginx 切到 openresty 运行 + 对外域名改用 gczx.egova.com.cn。本地产物全在 `D:\git\gczx-openresty\`。

## 服务器现状（2026-06-08）
- `ssh root@gczx.egova.com.cn` 免密直连，主机名 egova-jszcb，Rocky Linux 10.1，阿里云 ECS（内网 172.21.133.146 / EIP 8.160.174.102，DNS gczx.egova.com.cn→8.160.174.102）
- **当前实际跑系统 nginx 1.26.3**（`/usr/sbin/nginx`，systemd nginx.service，enabled=disabled active=active）；openresty 1.31.1.1 已装在 `/usr/local/openresty`（rpm 自带 unit `/usr/lib/systemd/system/openresty.service`，未启用），主配置还是 826B 默认模板
- conf.d 路径 `/etc/nginx/conf.d/`，**系统 nginx 和 openresty 共享同一 conf.d**（主配置都 include /etc/nginx/conf.d/*.conf），所以换引擎不动业务配置
- 已跑业务（与 demo 同款）：china-insight-board 前端(root /opt/china-insight-board/app/dist) + proxy: redmine-assist 8765 / captcha-ocr 8089(docker) / scan-webhook 8093(docker) / vuln-bridge 8092(uvicorn) / dailyreport 8080(docker) / admin+oauth+mcp 3001 / api 8787 / ecs-webhook 8899 / coding-plan 静态
- 出网 OK（LE API 200、github 200），入站 80/443 公网可达

## 已完成 ✅
1. 备份：`/root/gczx-backup/nginx-conf-20260608-180542.tar.gz`（含 /etc/nginx + openresty conf + ssl）
2. 装 acme.sh v3.1.4（`/root/.acme.sh/`，email phneixfh@gmail.com，已装 cron）
3. 签发 gczx.egova.com.cn 证书（LE ECC，webroot /var/www/acme），装到 `/etc/openresty/ssl/gczx.egova.com.cn.{fullchain.pem,key}`，有效期 2026-06-08~09-06，ARI 续期窗口 2026-08-07
4. **变更①换域名完成**：新 `/etc/nginx/conf.d/gczx.egova.com.cn.conf`（server_name+证书改 egova，所有 location 原样，含 80→443 跳转 + acme-challenge webroot）；旧 `gczx.majq1990.asia.conf` → `.disabled`；删临时 acme-egova.conf；`nginx -t` 通过 + reload
5. 公网验证通过：https 200 + ssl_verify=0（真证书）、captcha-ocr 200、80→301

## 待做 ⬜（变更②换引擎，唯一有中断风险的一步）
1. 写 openresty 主配置 `/usr/local/openresty/nginx/conf/nginx.conf`：参考 demo 风格但适配 gczx——user nginx / worker_connections 65535 / log_format **main+detail**（业务 conf 引用了 main，必须定义）/ map status mainflag+detailflag / map http_upgrade connection_upgrade(websocket) / gzip 全套 / server_tokens off / proxy_hide_header Server+X-Powered-By / lua_package_path / `include /etc/nginx/conf.d/*.conf`；**不要**照搬 demo 的 stream minio、/egova/log 路径(gczx 用 /var/log/nginx)、bi_bridge lua_shared_dict
2. `/usr/local/openresty/nginx/sbin/nginx -t` 验证主配置
3. 切换：`systemctl stop nginx && systemctl disable nginx && systemctl enable --now openresty`（回滚：openresty 起不来就 `systemctl start nginx`）
4. 验证 https://gczx.egova.com.cn 各 location（本机 curl 校验证书+业务）
5. acme 续期 reloadcmd 改 openresty：`acme.sh --install-cert -d gczx.egova.com.cn --ecc ... --reloadcmd "systemctl reload openresty"`

## 教训
- 本会话工具调用频繁 malformed（上下文过长+大参数 JSON 抖动），变更②这种"停了再起"操作建议在干净会话做；复杂命令一律 Write 脚本→scp→`tr -d '\r' < f | bash` 执行（见 [[feedback_long_yaml_writefile]]）
- demo 参考：openresty 1.19.3.1 在 /usr/local/openresty，主配置 include /etc/nginx/conf.d/*.conf + *.upstream，证书 ZeroSSL
