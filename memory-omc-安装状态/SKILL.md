---
name: memory-omc-安装状态
description: [memory] oh-my-claudecode v4.13.6 已以 preserve 模式装到全局，访问入口与文件位置
---

<!-- Auto-generated by skills-manager memory module.
     Source: C:\Users\majq1\.agent-memory\omc_install.md
     Do not edit here — edit the source and run `skills-manager-cli memory sync`. -->

**Memory type**: unspecified

OMC v4.13.6 已通过 plugin marketplace 装好，**preserve 模式**（不污染原有 CLAUDE.md）。

**访问方式**：所有命令必须用插件命名空间，如 `/oh-my-claudecode:autopilot`、`/oh-my-claudecode:team`、`/oh-my-claudecode:ralph`、`/oh-my-claudecode:setup`。直接 `/setup` 是 Unknown command。

**文件位置**：
- Marketplace 缓存：`~/.claude/plugins/marketplaces/omc/`（37 MB，5512 文件）
- Plugin 缓存：`~/.claude/plugins/cache/omc/oh-my-claudecode/4.13.6/`
- OMC CLAUDE 内容：`~/.claude/CLAUDE-omc.md`（被原 CLAUDE.md 通过 `<!-- OMC:IMPORT:START -->` 块 import）
- 原 CLAUDE.md 备份：`~/.claude/CLAUDE.md.backup.2026-05-06_182145`

**Why**：用户已有完整的 ~/.claude/CLAUDE.md（钉钉/RTK/wechat-acp 等），不能被 OMC 覆盖。preserve 模式下 plain `claude` 命令仍走原配置，`omc` 命令才强制加载 OMC 配置。

**Setup 已全部完成（4/4 phase）**：
- HUD 状态栏：`~/.claude/hud/omc-hud.mjs` + settings.json `statusLine` 已配（重启 Claude Code 后显示）
- 默认并行模式：`ultrawork`（写在 `~/.claude/.omc-config.json`）
- Team 默认值：3 agents、claude provider、Auto 显示模式
- omc CLI：装在 `C:\Users\majq1\AppData\Roaming\npm\omc`
- MCP 向导：用户主动跳过（已有 mem0/tavily/dingtalk/github 等）
- 关键文件：`~/.claude/.omc-config.json`（OMC 配置）、`~/.claude/CLAUDE-omc.md`（OMC 主配置）

**How to apply**：用户提到 OMC 命令时，记得加 `oh-my-claudecode:` 前缀。说要"重装/修复 OMC"时跑 `/oh-my-claudecode:omc-doctor` 或 `:omc-setup --force`（force 才会重跑全部 phase；默认会问"只更新 CLAUDE.md"还是"重跑全部"）。omc CLI 装好了但**当前 shell 看不到 psmux/omc**——告诉用户重启 Claude Code 后才会同时拿到 PATH 和 statusLine。
