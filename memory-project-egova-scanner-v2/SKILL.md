---
name: memory-project-egova-scanner-v2
description: [memory] Personal memory: project-egova-scanner-v2
---

<!-- Auto-generated by skills-manager memory module.
     Source: C:\Users\majq1\.agent-memory\project_egova_scanner_v2.md
     Do not edit here — edit the source and run `skills-manager-cli memory sync`. -->

**Memory type**: unspecified

# egova-security-scanner 持久扫描机(2026-05-21 校准)

## 持久扫描镜像(当前唯一现存)

- **ImageId**: `m-2ze9kt7278jfv57c2d4e`
- **名称**: `安全扫描-20260521-v3`
- **Region**: cn-beijing,Architecture x86_64,大小 40 GiB
- **创建时间**: 2026-05-21 05:06 UTC (13:06 CST)
- **内含**(推断,未逐项校验):Ubuntu 22.04 LTS + docker 29.1.3(china mirror) + nuclei v3.8.0 + nmap 7.98 + ZAP stable + curl + debian:bookworm-slim + nuclei-templates volume(13516 文件) + egova-playwright:v1(pin playwright==1.48.0 + 清华源)
- **历史(已不存在,mem 之前记错)**:
  - ~~v1 `m-2ze5yc5u3qall5im71un`(安全扫描-20260520)~~ — aliyun 列表中**已不存在**,推测被覆盖/删除
  - ~~v2 计划名 `安全扫描-20260520-v2`~~ — 从未在 aliyun 列表出现过,跳过未建
- **核验方法**:`aliyun ecs DescribeImages --RegionId cn-beijing --ImageOwnerAlias self` 列表中只有 v3 这一条"安全扫描"开头的

## 现役扫描机

- 实例: `i-2ze9tc0xnesrxbqeq7uu` (spot ecs.e-c1m4.xlarge, ¥0.068/h)
- 公网: `101.200.233.112` / 内网: `10.0.0.188`
- VPC: `vpc-2ze5yg4iioqg6p8be1abs` / VSwitch: `vsw-2ze6dmyu5gjp9p9oes7qa` (cn-beijing-h)
- SG: `sg-2ze1pookadxppb0kx9q2`(复用 qijian SG,`ALL -1/-1 src=10.0.0.0/8 Accept` 同 VPC 内成员零配置互通)
- SSH 密钥: 本机 `~/.ssh/mjqegova-ed25519`,scanner 上 `/root/.ssh/qijian_key`

## 麒舰目标(同 VPC)

- Node1 主控/出口nginx: 公网 `101.200.85.159` / 内网 `10.0.0.187` — 7 个 web 应用 + nacos 8848 + java 多服务
- Node2 MySQL/Redis/TDengine: 公网 `101.200.180.202` / 内网 `10.0.0.186`
- Node3 PostgreSQL/TDengine3: 公网 `101.200.151.219` / 内网 `10.0.0.185`

## 7 个 SSO 应用(密码同步)

- **当前真实密码** = `Egova@123`(2026-05-21 13:00 经 login_ocr_v2 在 eurbanpro 自动改密生效,SSO 同步全 app)
- 改密前 = `eGova@2023Yhzx`(初始)
- **首登入口** = eurbanpro(系统强制改密);usercenter 不识别改密流程,失败会累加 loginlock
- 密码策略 JS 正则:`/^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[.~!@#$%^&*)(_+}{|:?><]).{8,}$/`
- **跳过的 3 app**(永久,运维问题):linglong(license 过期)/ evaluation(毕升授权无效)/ dataflow(usercenter 重定向白名单未配)
- **待扫 4 app**:dex / usercenter / eurbanpro / xuanzang
- **SSO cookie 复用**:eurbanpro 改密重登拿 `admin_sso_token` + `admin_sso_refresh_token`,nuclei `-H Cookie` 走这对 token 即可,不需每 app 单独登

## 编排脚本(scanner 上)

- `/root/qj-orch-full.sh`(早期,login 流程未含改密码,Phase D 失败)
- `/root/qj-orch-v2.sh`(中期,Phase C 40+ 并发 nuclei 把 scanner 卡死过 — 必须 `xargs -P 4` 限并发)
- v3 待整理:Phase C nuclei `-P 4`,Phase D 走 `login_ocr_v2`(eurbanpro 改密入口)+ Egova@123 + OCR 服务过验证码

## 后续报告硬性要求

- 报告里**必须提示**:"应用密码已被扫描程序统一改为 `Egova@123`"
- 钉钉推送必须从 Linux 端发(scanner)用 `python3 + json.dumps ensure_ascii=False + UTF-8`,绝不在 Windows Git Bash 直发(中文乱码)
- 验证码自动识别走 `https://demo.egova.com.cn/captcha-ocr/`(v2.1,Redmine key 鉴权,支持 JSON `image_base64` 和 multipart `file=@`)
