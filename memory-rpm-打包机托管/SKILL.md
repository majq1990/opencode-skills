---
name: memory-rpm-打包机托管
description: [memory] 会话"rpm打包环境"，101.200.127.49 麒麟 V10 aarch64 ECS，用户希望后续整个 RPM 打包环境准备/维护工作由 Claude 托管操作
---

<!-- Auto-generated by skills-manager memory module.
     Source: C:\Users\majq1\.agent-memory\project_rpm_build_env.md
     Do not edit here — edit the source and run `skills-manager-cli memory sync`. -->

**Memory type**: unspecified

# RPM 打包机（aarch64）

**会话目标**：用户在 101.200.127.49 搭建 RPM 安装包（.rpm 分发包）打包机，后续用于给各类服务器生产基础环境 RPM 包。**希望整套环境准备工作托管给 Claude 自动执行**，用户迟点会说明如何通过 aliyun-cli 自行获取此 ECS 配置。

**Why**：基础环境包要反复重建、跨机器复制，手工操作不稳定。托管给 Claude = 可脚本化、可复现、可审计。

**How to apply**：
- 后续凡涉及这台机器的 yum 安装、rpmbuild 目录初始化、mock/createrepo/签名配置等，都应由我主动列计划 → 执行 → 记录命令到该 memory，而不是等用户敲命令。
- 每次新阶段完成后更新此文件的"已完成"清单。
- 跟踪用户手工操作：轮询 `~/.bash_history`（需提醒用户 `history -a` 落盘，或建议配 `PROMPT_COMMAND='history -a'`）。

## 连接信息
- SSH：`ssh root@101.200.127.49 -p 22`，密码 `Egova@123`
- 主机名：`iZ2zeb8aupetavddngqw5uZ`（阿里云 ECS 命名规则）
- 系统：Kylin Linux Advanced Server V10 (Halberd) aarch64
- 硬件：ARMv8 4C / 14Gi RAM / 38G 根分区（/dev/nvme0n1p3）/ 无 swap
- BIOS：Alibaba Cloud（virt-rhel7.6.0）

## 打包目标
**RPM 安装包**（不是 Python/Java 应用构建）——即写 spec、跑 rpmbuild 产出 `.rpm` 文件供分发。

## 已完成（2026-04-16）
- `yum groupinstall "Development Tools"` —— gcc 7.3.0 / g++ 7.3.0 / make 4.3 / autoconf 2.71 / automake 1.16.2 / libtool 2.4.6 / rpmbuild 4.15.1 / git 2.33.0 / wget / curl 全部到位
- `rpmdev-setuptree` —— `~/rpmbuild/{BUILD,RPMS,SOURCES,SPECS,SRPMS}` 已建

## 待办（Claude 已建议）
- `rpmlint` — spec 规范检查
- `yum-utils` — `yumdownloader` / `yum-builddep`
- `createrepo_c` — 生成 yum repo
- `mock` — 干净 chroot 构建隔离
- `rpm-sign` + `gnupg2` — 包签名
- （按包类型补充）Python/Java/Go/内核模块对应 devel 包

## 用户操作追踪
最近一次拉取 `~/.bash_history` 显示：用户先手动 groupinstall，然后调整了 `cloud.cfg` 和 grub 默认引导项，做了一次 reboot。与 RPM 打包无直接关系，属于系统层预处理。
