# 公钥接口 404 -- 本 skill 无法处理

你提到的是公钥接口返回 404 的排查问题，这属于**故障归因与排查**场景，不在本 skill（egova-eurbanpro-thirdparty-api-reference）的处理范围内。

## 本 skill 能做的

我可以帮你确认公钥接口的正确路径和请求方式。根据路由规则，公钥接口属于**认证接口**，对应文档为 `references/auth-apis.md`。但当前该引用文件缺失，无法提供具体的接口路径、参数和示例。

## 建议转交

请使用 **troubleshooting skill**（第三方排障技能）来处理此问题。该技能专门用于：
- 已出现报错（如 404），需要根据现象倒推原因
- 排查配置是否正确、接口路径是否匹配
- 定位环境、版本、部署等层面的故障

## 如果你需要

- **确认公钥接口的正确路径和请求方式** -> 可以回到本 skill，等 `references/auth-apis.md` 文件就绪后，我可以给出具体的 action、接口地址和最小请求示例。
- **排查 404 原因** -> 请使用 troubleshooting skill。
