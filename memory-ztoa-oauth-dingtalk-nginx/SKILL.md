---
name: memory-ztoa-oauth-dingtalk-nginx
description: [memory] demo.egova.com.cn 上 ztoa MCP /oauth/dingtalk/ 反代规则、易丢失原因与恢复方式
---

<!-- Auto-generated by skills-manager memory module.
     Source: C:\Users\majq1\.agent-memory\reference_ztoa_oauth_dingtalk_nginx.md
     Do not edit here — edit the source and run `skills-manager-cli memory sync`. -->

**Memory type**: reference

demo.egova.com.cn 上 ztoa MCP 的钉钉免登跳转依赖 `/etc/nginx/conf.d/default.conf` 里这一段反代（upstream 是 `ztoa-mcp-http:3001`，跟 `/mcp/` 是同一个进程但走独立 nginx location）：

```
location /oauth/dingtalk/ {
    proxy_pass http://127.0.0.1:3001;
    proxy_set_header Host 127.0.0.1:3001;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;
    proxy_http_version 1.1;
}
```

**易踩坑**：
- 这段每次改 default.conf 时容易被"顺手删"（2026-07-10 已经在 7-8 那次改动里失踪一次，导致 https://demo.egova.com.cn/oauth/dingtalk/login → 404）
- `default.conf.fixed` 里这个块**自身 `}` 缺失**（挨着 `/project-export/` 直接接过去了），不能拿来当模板
- 想恢复找 `default.conf.bak.20260522-v22` / `.bak.20260508`——那两份里的 8 行块是干净的、闭合的
- upstream 3001 是 `ztoa-mcp-http` 进程；直接 `curl http://127.0.0.1:3001/oauth/dingtalk/login` 期望 302

**验证**：`curl -I https://demo.egova.com.cn/oauth/dingtalk/login` → HTTP 302 = 正常；404 = nginx 里 location 又被删了

相关：[[reference_ztoa_endpoints]]、[[project_ztoa_mcp]]、[[reference_v3_infra]]
