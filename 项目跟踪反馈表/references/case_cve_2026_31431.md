# 首次实战：CVE-2026-31431 漏洞修复跟踪反馈表（2026-05-06）

> 这是 `project-tracking-table` skill 的 v0 实战样本，未来同类表直接拷贝结构改参数即可。

## 输出

- baseName: `CVE-2026-31431漏洞修复跟踪反馈表`
- baseId: `vy20BglGWOeOolZDc02KElDqJA7depqY`
- tableId: `hERWDMS`（默认表 rename）
- viewId: `qvGDAH2`
- URL: https://docs.dingtalk.com/i/nodes/vy20BglGWOeOolZDc02KElDqJA7depqY
- 行数：586（去掉测试行后）
- 已写入：项目/大区/区域/项目经理/大区工程总/省份工程总（前 6 列），其他待填表人补

## 字段表（完整 16 列）

| fieldId | type | fieldName | 用途 |
|---|---|---|---|
| `01ZM8y7` | primaryDoc | 项目 | 主键，从 ztoa「项目名称」拷过来 |
| `iRWA4SK` | singleSelect | 大区 | 8 选项（华北一区/华北二区/华中大区/东南大区/华南大区/西南大区/西北大区/华北大区（测试）） |
| `81LkWjk` | text | 区域 | 如"山东区域" |
| `4dCZcZ5` | user | 项目经理 | 钉钉成员，从 PM 姓名经部门去歧映射 |
| `y9GHX8p` | user | 责任人 | （用户在前端手动加的，与项目经理同值） |
| `eldbJ0u` | singleSelect | 脚本扫描结果 | 4 选项：`PATCHED 已修复`/`MITIGATED 已缓解`/`VULNERABLE 受影响`/`UNKNOWN 未知` |
| `coeJxRl` | attachment | 脚本扫描结果截图 | 用户在前端加的 |
| `UYCINAq` | text | 操作系统名称 | 待填，如 `KylinOS` |
| `3CiRAwd` | text | OS版本 | 待填，如 `V10 SP3` |
| `yx5p5Kk` | text | 内核版本(uname -r) | 待填 |
| `diu5pTA` | singleSelect | 反馈状态 | 4 选项：`未反馈`/`已反馈`/`反馈中`/`无法反馈`，初值都是"未反馈" |
| `C1xwhaT` | text | 反馈备注 | |
| `YQWBWFh` | date | 反馈时间 | format `YYYY-MM-DD` |
| `s3PrLFJ` | text | 图片和附件理解 | 用户在前端加的 |
| `2grSiAH` | user | 大区工程总 | **应隐藏只读** |
| `RThSPCf` | user | 省份工程总 | **应隐藏只读** |

视图列序：`项目 / 大区 / 区域 / 项目经理 / 责任人 / 脚本扫描结果 / 截图 / OS 名称 / OS 版本 / 内核 / 反馈状态 / 备注 / 时间 / 图片理解 / 大区工程总 / 省份工程总`

## 关键统计

| 指标 | 值 |
|---|---|
| ztoa 全量交付项目 | 1851 |
| 状态=打开 | 599 |
| 打开 + 在职 PM | 587 |
| 写入后删测试 | 586 |
| PM 姓名 unique | 178 |
| 钉钉单匹配 | 175 |
| 钉钉同名（按部门去歧） | 2（吴迪/李岩，全部解决） |
| 钉钉无账号 | 1（李覃测试，已删） |
| 大区分布 | 华中169 / 东南108 / 华北二92 / 华北一80 / 西北55 / 西南46 / 华南36 / 测试1 |
| 琅琊榜映射覆盖 | 585/587（业务部+测试 2 个边缘 case） |

## 4 类扫描结果（来自社区脚本）

社区工具：`ochebotar/copy-fail-CVE-2026-31431-detection-probe`
退出码 → 结果：

| 退出码 | 结果 | 含义 |
|---|---|---|
| 0 | PATCHED | 内核已升到补丁版本 |
| 0 | MITIGATED | 内核版本受影响但有 initcall_blacklist 等缓解 |
| 1 | VULNERABLE | 命中漏洞且无缓解 |
| 2 | UNKNOWN | 无法解析内核或发行版 |

## 同名去歧的实战示例

**吴迪：** 钉钉两人，title 不同 → 选 `项目经理` 那位（不是 `高级销售经理`）
- userId=`16244934427746239` ✓ 选中

**李岩：** 钉钉两人，title 都是 `项目实施工程师` → 用 ztoa 项目"区域=辽宁区域"匹配钉钉 `depts[].deptName='辽宁区域'`
- userId=`16984255030023919` ✓ 选中（辽宁区域）
- userId=`17516154653361726` ✗ 排除（山东区域）

## 用户在前端的手动调整（保留观察）

跑完脚本后用户在钉钉前端额外加了：
- `coeJxRl` 附件字段「脚本扫描结果截图」
- `s3PrLFJ` 文本字段「图片和附件理解」（疑似 AI 字段）
- `y9GHX8p` user 字段「责任人」（值同步到项目经理）
- 把 `eldbJ0u` 从「扫描结果」rename 成「脚本扫描结果」

下次建表时这几个字段可以加进默认模板，省去手动配置。
