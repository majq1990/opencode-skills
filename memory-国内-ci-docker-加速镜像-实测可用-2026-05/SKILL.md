---
name: memory-国内-ci-docker-加速镜像-实测可用-2026-05
description: [memory] docker.io 拉镜像、GitHub release 文件、rustup/cargo crate 在国内 CI/服务器上要走的镜像源
---

<!-- Auto-generated by skills-manager memory module.
     Source: C:\Users\majq1\.agent-memory\reference_china_ci_mirrors.md
     Do not edit here — edit the source and run `skills-manager-cli memory sync`. -->

**Memory type**: unspecified

# Docker Hub

`/etc/docker/daemon.json` 配 `registry-mirrors`：

```json
{
  "registry-mirrors": [
    "https://docker.1ms.run",
    "https://docker.xuanyuan.me",
    "https://hub.rat.dev",
    "https://docker.mirrors.ustc.edu.cn"
  ]
}
```

# GitHub release / raw.githubusercontent.com

实测 2026-05-08 速度：
- ✅ `https://ghfast.top/<gh-url>` — 95MB 17s（约 5MB/s），稳定
- ⚠️ `https://gh-proxy.com/<gh-url>` — 限速 5MB 左右，慢
- ❌ `https://ghproxy.cn/<gh-url>` — DNS 解析 ghproxy.cfd 失败（CentOS 7）
- ❌ `https://ghproxy.net/<gh-url>` — 不稳

格式：在 GitHub URL 前贴上 mirror 域名即可，例如：
`https://ghfast.top/https://github.com/actions/runner/releases/download/v2.334.0/actions-runner-win-x64-2.334.0.zip`

# Rust toolchain + crates

`rsproxy.cn`（字节跳动）最快。Dockerfile 里：

```Dockerfile
ENV RUSTUP_DIST_SERVER=https://rsproxy.cn
ENV RUSTUP_UPDATE_ROOT=https://rsproxy.cn/rustup
RUN curl -fsSL https://rsproxy.cn/rustup-init.sh | sh -s -- -y --profile minimal --default-toolchain stable
```

cargo crates 走 sparse 索引：
```toml
# ~/.cargo/config.toml or /usr/local/cargo/config.toml
[source.crates-io]
replace-with = "rsproxy-sparse"
[source.rsproxy-sparse]
registry = "sparse+https://rsproxy.cn/index/"
[net]
git-fetch-with-cli = true
```

# npm

`registry.npmmirror.com`（淘宝）最快。`npm config set registry https://registry.npmmirror.com` 或 Dockerfile RUN 里设。
