---
# This playbook deploys the python2
- name: deploy and configure the python2
  hosts: "{{ host }}"
  gather_facts: "{{ gather_facts }}"
  remote_user: root
  # 优先级小于role/vars下定义的变量
  vars_files:
    - ./group_vars/all.yml
  roles:
    #- common
    - local_repo
    - python2
