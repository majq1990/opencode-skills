---
name: memory-服务器登录信息
description: [memory] 常用服务器的 SSH 登录账号、用途汇总
---

<!-- Auto-generated by skills-manager memory module.
     Source: C:\Users\majq1\.agent-memory\reference_servers.md
     Do not edit here — edit the source and run `skills-manager-cli memory sync`. -->

**Memory type**: unspecified

## majq1990.asia
- **公网 IP**：43.163.81.173（腾讯云 **Lighthouse 轻量应用服务器** `lhins-anwwgx0h`，region `ap-singapore`，zone `ap-singapore-1`；1TB/月流量）
- **Tailscale IP**：`100.103.28.84`（hostname `majq1990-asia`，账号 `majq199006@gmail.com`；同时是 tailnet 的 **exit node**，手机端可选它当翻墙出口，v2rayNG 变可选）
- **账号**：`root`
- **SSH（2026-07-15 起）**：**端口 22022**（公网 22 已下线，sshd_config `Port 22022` only；云侧防火墙 22 规则已删）
  - 首选走 tailscale：`ssh -p 22022 root@100.103.28.84`
  - 应急走公网：`ssh -p 22022 root@majq1990.asia`（22022 已加入云侧防火墙 0.0.0.0/0）
  - 本机 `~/.ssh/config` 建议加 `Host majq1990` alias 省事
- **跳板备用**：先 `ssh root@demo.egova.com.cn`，再 `ssh -p 22022 -o StrictHostKeyChecking=no -i /root/mjqegova root@majq1990.asia`
- **用途**：
  - `/opt/notebooklm-daily-report/` — NotebookLM 自动日报/周报项目（confirm-api 已改 `PUBLIC_BASE_URL=http://100.103.28.84:8765`，钉钉链接走 tailscale）
  - `/root/ql/` — 青龙面板（5700 只绑 tailscale + 127.0.0.1）
  - `claude-code` 容器（node:20）常驻，复用跑 dws skill
- **环境**：CentOS 7，GLIBC 2.17 太老，宿主跑不了 node/claude，必须 Docker
- **防火墙分两层**：
  - **腾讯云 Lighthouse 防火墙**（外层，tccli 或 `lighthouse` 服务管理，详见 [[reference_tencent_lighthouse]]）—— 2026-07-15 从 25 条清到 11 条，只留：22022/UDP全放/2053/8443/23456/80/443/ICMP
  - **服务器内**：**没跑** firewalld/iptables service；`/usr/local/qcloud/YunJing/`（腾讯云主机安全「云镜」）自维护 `YJ-FIREWALL-INPUT` 链 + `YJ-GLOBAL-INBLOCK/OUTBLOCK` ipset（2026-07-15 已封 11598 IP）。加自定义规则要与 YJ 链共存 + docker port map 用 DOCKER-USER 链
- **公网监听（2026-07-15 收紧后）**：22022(ssh) / 80(nginx) / 443+2053+8443+23456(xray REALITY)。5700(青龙)、8765(confirm-api) 均已从公网撤下，只绑 100.103.28.84 + 127.0.0.1
- **docker→tailscaled 启动顺序**：`/etc/systemd/system/docker.service.d/tailscale.conf` 加了 `After=tailscaled.service Wants=tailscaled.service`，防止重启后容器绑 100.x 失败
- **历史：43.163.81.173 自撞 SSH 5041 次**：docker 端口回环（hairpin NAT），容器出网 SNAT 后源 IP=eth0，看起来"自撞自"。SpiderFoot（`/opt/spiderfoot/` 空目录）不是元凶——db/日志空。不构成风险（key-only）

## demo.egova.com.cn
- **账号**：`root` 免密直连（`ssh root@demo.egova.com.cn` 或 `ssh demo.egova.com.cn`）
- **OS**：CentOS 7，Python 3.6，nginx 是 OpenResty
- **现有服务**（按部署顺序）：
  - 8091 端口 — Skill 分发服务器（详见 `reference_skill_server.md`）
  - 127.0.0.1:8899 — ECS 自动采购 webhook（详见 `project_ecs_auto_procurement.md`）
  - 127.0.0.1:8000 — 钉钉日报统计平台
- **nginx 主域**：默认 server 块 server_name=localhost，HTTPS 走 acme.sh 自动证书 `/root/.acme.sh/demo.egova.com.cn/`
- **nginx location 模式**：default.conf server 块末尾有 `include /etc/nginx/conf.d/*.location;`，新建 `*.location` 文件自动加载
- **磁盘**：`/` 148G，2026-04-27 已用 91%，`/egova/MediaRoot` 占 75G
- **不允许删除原有目录/服务**（业务在跑）

## gczx.egova.com.cn
- **账号**：`root` 免密直连（`ssh root@gczx.egova.com.cn`，本机 ~/.ssh key 已授权）；公网 8.160.174.102
- **主机名**：`egova-jszcb`（工程支持中心 / 技术支持部）
- **OS**：Rocky Linux 10.1 (Red Quartz)，docker + openresty
- **Web 引擎**：2026-06-09 起 **openresty 1.31 接管**（系统 nginx 已 mask）；业务配置 `/etc/nginx/conf.d/gczx.egova.com.cn.conf`（openresty 也 include 此目录）；证书 `/etc/openresty/ssl/`；⚠️ openresty pid 目录 `/usr/local/openresty/nginx/logs/` 必须存在否则启动失败
- **已迁 AI 服务**（docker）：dingtalk-report-v3(8080)、scanner-webhook(8093)、redmine-assist(8765)、captcha-ocr:2.2(8089)；/opt 下 vuln-bridge/ztoa-mcp/redmine-mcp 部分未起
- **注意**：docker.io 直连超时（未配国内 mirror），拉公共 base 镜像会失败
- 详见 [[gczx AI 服务迁移 + openresty 接管]]（project_gczx_ai_migration.md）
- 2026-06-08 首次登录确认可达

## 101.200.127.49
- 见 `project_rpm_build_env.md`：麒麟 V10 aarch64 RPM 打包机
