---
name: memory-2026-07-17-log-platform-dau
description: [memory] Personal memory: 2026-07-17-log-platform-dau
---

<!-- Auto-generated by skills-manager memory module.
     Source: C:\Users\majq1\.agent-memory\2026-07-17-log-platform-dau.md
     Do not edit here — edit the source and run `skills-manager-cli memory sync`. -->

**Memory type**: unspecified

# 2026-07-17 Egova Log Platform DAU Context

User is building a local DAU observatory for the Elasticsearch/Kibana log platform at `172.28.28.4`.

Key source facts:
- Kibana is reachable at `http://172.28.28.4:5601/app/dev_tools#/console/shell`.
- Elasticsearch is reachable at `https://172.28.28.4:9200`.
- DingTalk document `日志平台` was read through `dws doc read`; it documents Filebeat shipping `system-logs-*`, `nginx-logs-*`, `tomcat-logs-*`, and `java-app-logs-*`.
- `java-app-logs-*` is the Java application log source.
- `tomcat-logs-*` is Tomcat local access/catalina log source.
- Do not turn off Tailscale/exit node while assisting, because it can break Codex/GPT connectivity.

Evidence found:
- `java-app-logs-2026.03.08` has 2601 docs, mostly `linglong` and `ibility`.
- Current Java mappings only expose generic fields such as `@timestamp`, `app_name`, `log_type`, `server_name`, `message`, and `log.file.path`; standard stable identity fields such as `user.id` are absent.
- Message markers like `username=` or `Token:` are sparse and unstructured, so they must not be used as authoritative DAU.
- `tomcat-logs-2026.03.01` has 43157 requests, all sampled status `404`, `tomcat.user` is `-`, and unique client IP is 1. Treat Tomcat as traffic/error quality, not DAU.

Metric decision:
- Business DAU should only be counted from stable Java business activity events with `user.id` plus qualifying event fields such as `event.dataset`, `event.action`, `event.outcome`, `tenant.id`, and `service.name`.
- Tomcat unique IP, Nginx unique IP, access count, and raw runtime messages are not DAU.
- Current state: business DAU is blocked until Java emits structured user activity logs.

Local implementation:
- Workspace app path: `C:\Users\majq1\Documents\Codex\2026-07-17\x-z-m`.
- App name: `egova-dau-observatory`.
- Start with `npm start` after setting `ES_URL`, `ES_USER`, `ES_PASSWORD`, `INSECURE_TLS=1`, and `PORT=4310`.
- Local URL: `http://localhost:4310/`.
- The app was changed from live ES querying to an offline extracted result dashboard.
- `jobs/extract-dau.mjs` extracts daily results from ES into `data/dau-store.json`.
- `scripts/run-yesterday-extract.ps1` runs yesterday extraction for Windows scheduling.
- The result page reads `data/dau-store.json`, so opening the dashboard shows precomputed daily values rather than scanning ES on page load.
- A 30-day backfill ending `2026-03-08` was extracted. The latest date `2026-03-08` has candidate DAU 1, and the 30-day max candidate DAU is 20. These are marked low-confidence candidate DAU because they come from Java message identity markers, not stable structured `user.id`.
- Updated later: storage was upgraded from JSON to SQLite at `data/dau.sqlite`, table `daily_metrics`, one row per date.
- `npm run extract:all` discovers every existing `java-app-logs-*` and `tomcat-logs-*` date in ES and stores one daily row.
- `npm run extract:resume` continues only dates not already in SQLite.
- `scripts/run-yesterday-extract.ps1` is for scheduled daily extraction of yesterday.
- Full backfill completed into SQLite: 228 stored days, first `2025-07-25`, latest stored `2026-07-17` (partial current-day row), latest complete dashboard date `2026-03-08`, max candidate DAU 53, 129 nonzero days.
- Dashboard intentionally defaults to latest complete date and excludes today's partial row from the default trend.
- On 2026-07-17, the user asked what to do when no user identifier exists. Decision: do not rename IP/session/token/PV as DAU. Show them as separate low-trust proxy activity metrics.
- Implementation updated:
  - `daily_metrics` now has `proxy_json`.
  - `jobs/extract-dau.mjs` computes session/token/Authorization hashes from Java messages, without storing raw token values.
  - Tomcat unique IP remains traffic reach only.
  - Request volume remains volume only.
  - The dashboard now displays candidate DAU, proxy activity, Tomcat requests, and error rate separately.
  - Full backfill was rerun for 229 discovered ES dates. Latest complete default is guarded against a large tail gap, so homepage defaults to `2026-03-08`; stored rows include tail dates `2026-07-17` and `2026-07-18`.
  - For `2026-03-08`, candidate DAU is 1 and proxy activity is 2 session/token hashes from 2 token-bearing Java messages. This explains why “1 user” is not necessarily true business DAU; identity coverage is simply too sparse.
  - For `2025-11-14`, candidate DAU is 53 and proxy session/token activity is 5.
- Browser validation passed at `http://localhost:4310/`; screenshot saved as `outputs/dau-observatory-proxy.png`.
- On 2026-07-22, user confirmed newer ES logs exist and asked to refresh this week's DAU. Ran `npm run extract -- --end 2026-07-22 --days 3`, refreshing `2026-07-20` through `2026-07-22` into SQLite.
  - `2026-07-20`: candidate DAU 6, proxy session/token activity 3, Java docs 1754, Tomcat requests 95348, Tomcat unique IP 2, Tomcat error rate 45.56%.
  - `2026-07-21`: candidate DAU 9, proxy session/token activity 2, Java docs 2108, Tomcat requests 98676, Tomcat unique IP 2, Tomcat error rate 44.20%. This is the latest complete default dashboard date after refresh.
  - `2026-07-22`: partial current-day candidate DAU 3, proxy session/token activity 2, Java docs 895, Tomcat requests 47501, Tomcat unique IP 2, Tomcat error rate 44.55%.
  - `lib/dau-db.mjs` default latest date rule was simplified to use latest stored date before Asia/Shanghai today, so the dashboard now defaults to `2026-07-21` instead of the old pre-gap date `2026-03-08`.
  - Browser validation passed; screenshot saved as `outputs/dau-observatory-week-2026-07-22.png`.
- On 2026-07-22, inspected current ES user-identification signals. Added reusable probes:
  - `scripts/probe-user-signals.mjs`
  - `scripts/probe-distinct-user-signals.mjs`
  - `scripts/probe-nginx-marker-precision.mjs`
  - `scripts/sample-redacted-user-signals.mjs`
  - Included them in `npm run check`.
- Findings for latest complete date `2026-07-21`:
  - `java-app-logs-2026.07.21`: 2108 docs; only 18 username/userName docs; distinct username 9; token distinct 2. Java remains too sparse for true DAU.
  - `tomcat-logs-2026.07.21`: 98676 docs; `tomcat.user` is `-`; client IP only `172.28.28.3` and `127.0.0.1`; not usable as user identity.
  - `nginx-logs-2026.07.21`: 21976 docs; message contains strong candidate user/session signals: username/userName markers 813 docs, remUsername marker 605 docs, SESSION 1304 docs, sl-session 1233 docs, JSESSIONID 1487 docs, eUrbanToken 681 docs, admin_sso_token 733 docs, cas_token 938 docs.
  - Redacted distinct counts on Nginx `2026-07-21`: username 105, remUsername 68, userId 1, session 272, token 177.
  - Current best candidate for user-level DAU is Nginx access.log message username/userName/remUsername, with session/token as low-trust proxy only. Do not store raw token/session values.
- Findings for partial current date `2026-07-22`:
  - Java distinct username 3.
  - Nginx distinct username 46, remUsername 20, session 110, token 73.
- On 2026-07-22, user asked to apply the Nginx-based DAU recommendation and later noticed the latest daily report jumped back to `2026-03-08`.
  - Cause of the visible jump: `/api/range` used the last N stored rows rather than a natural date window; because the data has a gap from `2026-03-08` to `2026-07-17`, the trend could mix old March rows with July rows. Fixed `server.mjs` so range uses natural latest-date minus N days.
  - Implemented Nginx as primary candidate DAU in `jobs/extract-dau.mjs`: `extractNginx()` parses `username/userName`, `remUsername`, `userId`, `SESSION/sl-session/JSESSIONID`, and token-family markers from Nginx messages; only hashes/counts are stored, never raw values.
  - Added SQLite `nginx_json` column and API/frontend fields for Nginx candidate DAU, remUsername, Java candidate users, and session/token activity.
  - Rebuilt `public/index.html` and `public/app.js` to show "Nginx 候选日活" as the hero metric.
  - Added ES timeout/curl fallback and `--nginx-only` extract mode, but ES/Node fetch remained unstable during full re-extraction. Materialized already verified Nginx probe counts into SQLite for `2026-07-21` and `2026-07-22`.
  - Current verified dashboard values: latest complete date `2026-07-21`; Nginx candidate DAU 105; remUsername auxiliary 68; Nginx session/token activity 272. Partial `2026-07-22`: Nginx candidate DAU 46; remUsername 20; session/token activity 110.
  - Browser validation passed at `http://localhost:4310/?v=nginx-dau`: latest date 2026-07-21, hero value 105, page mentions Nginx, no 2026-03-08 text, no JS errors. Screenshot saved as `outputs/dau-observatory-nginx-2026-07-22.png`.
- On 2026-07-22, user clarified that server deployment must initialize by directly pulling all historical ES data and building the history database.
  - Added `jobs/init-history.mjs`.
  - Added npm scripts:
    - `npm run init:history` for full first-deployment history build.
    - `npm run init:history:resume` for interrupted initialization.
    - `npm run init:history:nginx` for refreshing Nginx-derived DAU fields while reusing existing Java/Tomcat rows.
  - Added `scripts/init-history.sh` and `scripts/init-history.ps1`.
  - `init-history` creates `data/init-history.lock` while running, calls `jobs/extract-dau.mjs --all --progress`, writes one row per discovered date to `data/dau.sqlite`, then prints a summary with dayCount/firstDate/latestDate/latestStoredDate.
  - Updated `README.md` and `docs/server-deploy.md` so first deployment uses `npm run init:history` before starting/scheduling the service.
- Later on 2026-07-22, user clarified the target server is Kylin V10, x86 CPU, and can only be deployed offline.
  - Treat Kylin V10 x86 as Linux `x86_64`.
  - Added `scripts/build-offline-package.ps1` and npm script `npm run package:offline`.
  - The offline package excludes `.env`, `data/`, `node_modules/`, `dist/`, `outputs/`, `work/`, `.agents/`, and `.codex/`.
  - User later warned the server may only have Node 16. Initially attempted Node16 compatibility, but smoke test showed the app imports Node's built-in `node:sqlite`, which is unavailable in Node16. Bare-metal deployment therefore requires Node `>=22.5`; if the host only has Node16, use the container path.
  - Added `lib/http-json.mjs`, a Node 16-compatible native `http/https` JSON requester, and replaced all Node-side ES `fetch` calls in server/jobs/scripts. Browser-side `public/app.js` still uses browser `fetch`, which is not affected by server Node version.
  - Generated and validated the current bare-metal fallback package at `dist/egova-dau-observatory-offline-20260722-172113.zip`; verified it excludes `.env` and `data/` and includes `lib/http-json.mjs`.
  - Updated `docs/server-deploy.md` with Kylin V10 x86 offline bare-metal fallback: install Node 24.14.0 Linux x64 tarball under `/opt/node24`, unzip project under `/opt/egova-dau-observatory`, create `.env`, run `npm run init:history`, then run with PM2 or systemd; added systemd service/timer alternatives for offline servers.
  - Current app has no third-party runtime npm dependencies, so the project package can run offline without `npm install`; if future dependencies are added, prepare a matching offline npm cache or bundled `node_modules`.
- On 2026-07-22, user asked whether containerized deployment is better. Recommendation: yes, prefer containerization for offline Kylin V10 if Docker/Podman is available, because it bundles Node 16 and avoids host Node version problems.
  - Added `Dockerfile`; after smoke testing, changed it to `node:24.14.0-alpine` because Node16 lacks `node:sqlite`.
  - Added `.dockerignore`.
  - Added `docker-compose.yml` with service `dau`, image `egova-dau-observatory:node24`, port `4310:4310`, `.env`, and bind mount `./data:/app/data`.
  - Added `scripts/build-container-offline-package.ps1` and npm script `npm run package:container`; it builds the image, saves `image.tar`, and zips it with `docker-compose.yml`, `.env.example`, and deployment docs.
  - Updated `README.md` and `docs/server-deploy.md` with container-first offline flow: `docker load -i image.tar`, create `.env`, `mkdir -p data`, `docker compose run --rm dau npm run init:history`, then `docker compose up -d dau`; daily extraction can run from host cron via `docker compose run --rm dau npm run extract`.
  - After user started Docker Desktop, pulled `node:24.14.0-alpine` via `docker.m.daocloud.io/library/node:24.14.0-alpine`, tagged it locally as `node:24.14.0-alpine`, built image `egova-dau-observatory:node24`, and generated validated container offline package `dist/egova-dau-observatory-node24-container-offline-20260722-171749.zip`.
  - Verified container package contents: `.env.example`, `docker-compose.yml`, `image.tar`, `server-deploy.md`; `image.tar` is about 56 MB.
  - Smoke test passed: ran `egova-dau-observatory:node24` on local port 4311 and `GET /api/bootstrap` returned HTTP 200. Node printed an experimental warning for `node:sqlite`, but service started successfully.
- User asked to record memory and plan handoff to opencode.
  - Added `docs/opencode-handoff.md`.
  - Handoff doc states the goal, preferred container package, server deployment commands, `.env` requirements without credentials, validation already completed, DAU metric definition, caveats, and immediate opencode checklist.
  - Opencode should prefer `dist/egova-dau-observatory-node24-container-offline-20260722-171749.zip` for Kylin V10 offline deployment.
  - Bare-metal fallback remains `dist/egova-dau-observatory-offline-20260722-172113.zip`, but only for Node `>=22.5`; Node16 host should use the container path.

Security note:
- ES credentials were intentionally not stored in this memory.

Elasticsearch retention preference:
- User wants ES data to remain untouched during normal operation unless disk pressure is high.
- Conservative cleanup guardrail: only consider deleting log indices older than 18 months.
- On 2026-07-17, ES disk usage was 46%, so no deletion was performed.
- `scripts/es-retention-cleanup.mjs` was added. It defaults to dry-run and only deletes with `--apply` when disk usage is above the configured threshold, or with emergency `--force`.
- Dry-run at 18 months / 85% threshold found 13 old tiny `nginx-logs-*` indices and deleted 0.
