---
name: memory-project-wukong-component-kg
description: [memory] 悟空组件知识图谱+大屏推荐复现路线，部署到 gczx 走钉钉认证
---

<!-- Auto-generated by skills-manager memory module.
     Source: C:\Users\majq1\.agent-memory\project_wukong_component_kg.md
     Do not edit here — edit the source and run `skills-manager-cli memory sync`. -->

**Memory type**: project

2026-06-09 立项：把悟空组件知识（能力/cardData schema/坑）探测成**可检索知识图谱**，改变大屏复现方式——拿到效果图**先组件推荐+用户确认**，再用 verified schema 落地，不再像本次党建大屏那样全靠试错。

**动因**：本次「党建引领网格治理」1:1 复现踩坑（Swiper11 列 key column1/column2 必须不同+showContentRadio='data'、BasicLine 格式靠猜、PUT 不支持要 fork、翻牌器选错），全因缺一份组件能力的可检索知识。

**已定决策**：
- 基建：轻量结构化目录(JSON 能力卡) + LightRAG 语义索引；精确 schema 永远回目录取，模型不得幻觉
- 部署：全栈上 **gczx.egova.com.cn**（Rocky10/8核30G/88G空闲/docker29+compose v5/openresty 在跑，证书 `/etc/openresty/ssl/gczx.egova.com.cn.fullchain.pem` 已就位，免密 root 直连）
- 调用走**钉钉认证**，参考 demo `/opt/ztoa-mcp` 范式：auth 骨架(dingtalk.py/whitelist.py/session.py/storage.py/audit.py/BearerAuthMiddleware)几乎原样搬，只换工具层为 KG
- 钉钉应用：**复用「通途mcp」(AppId c3bef0df…)**，钉钉开放平台追加回调 `https://gczx.egova.com.cn/wk-kg/oauth/dingtalk/callback`（手工控制台动作）
- ⚠️ gczx 已有 `/oauth/dingtalk/`+`/mcp/`+3001 = gczx 版 ztoa-mcp 槽位(当前没启动)，**wk-kg 必须用独立前缀 /wk-kg-mcp/ + 端口 3100，勿碰 3001**；neo4j 7687 / lightrag 9100 空闲

**KG 工具层**：recommend_components(视觉描述→候选+能力边界) / get_component_schema / search_components / list_components

**进度**：方案文档 `C:\Users\majq1\wukong-recon\COMPONENT-KG-ROUTE.md` 完整。
- 阶段1✅：6 张能力卡 `wukong-recon\kg\components\*.json` + `index.json`（Swiper11/IndexStatistic/BorderBox15/Time 已验证，BasicLine partial 待 live 复核，BasicBar11 未验证）。
- 阶段2✅：MCP 服务已建 **`D:\git\wk-kg-mcp\`**（git 本地仓 commit 87c9c79+清理）。auth 骨架逐字复用 ztoa(`D:\git\_ztoa_ref\` 是拉下来的参考源)。精确层 catalog.py 读能力卡(冒烟过:6卡+关键词召回准)、语义层 semantic.py LightRAG瘦客户端(禁用回退关键词)、5个工具(recommend_components/get_component_schema/search_components/list_components/get_write_pattern)、http_server 独立前缀。py_compile 全过。端口 3100。
- 阶段3✅(2026-06-10)：上线运行中。`/opt/wk-kg-mcp/`(项目)+`/opt/wk-kg-mcp/data/`(audit+sessions+whitelist_seed)+`/opt/wk-kg-mcp/logs/`+`/opt/wk-kg-mcp/scripts/ingest_kg.py`(灌库脚本)；PM2 跑 wk-kg-mcp(3100，online)，docker-compose 跑 wk-kg-neo4j(7687,4G)+wk-kg-lightrag(9100, deepseek-v4-flash + bge-m3 SiliconFlow API)；nginx 加 3 个 location 到 443 块(/wk-kg/oauth/dingtalk/ + /wk-kg-mcp/ + /wk-kg-health)，备份 `/etc/nginx/conf.d/gczx.egova.com.cn.conf.bak-20260609-*`；白名单 376 人同步；6 张能力卡灌库完成。**端到端 3 场景验证通过**：19区排名表→Swiper11；翻牌涨跌→IndexStatistic；24h 趋势→BasicLine+IndexStatistic 备选。所有 cannotDo 边界正确返回。
  - 用户登录 URL: https://gczx.egova.com.cn/wk-kg/oauth/dingtalk/login
  - MCP 端点: https://gczx.egova.com.cn/wk-kg-mcp/，Bearer token 90 天
  - 健康检查: https://gczx.egova.com.cn/wk-kg-health
  - 仅 3 个工具被实测：recommend_components / get_component_schema / get_write_pattern (search_components/list_components 同源未单独测)
- 阶段4✅(2026-06-10)：「悟空大屏生成」skill SKILL.md 已并入 wk-kg-mcp。改第 2 层流水线为「**组件推荐+确认**（必经）」，新增「🎯 wk-kg-mcp 集成」节（端点+登录+客户端配置 JSON+5工具表+推荐映射表样例），安全红线加一条「任何效果图复现必先经第 2 层推荐+用户确认，不许凭记忆装配」，参考资料指向 wk-kg-mcp 与 COMPONENT-KG-ROUTE.md。Claude Code 的 `~/.claude.json` mcpServers 加 "wk-kg" Bearer 配置；opencode 走 junction 已自动同步（两边 `SKILL.md` 都含 7 处 wk-kg-mcp 引用）。skill 仓 git push 待网络恢复（参 [[feedback_gh_api_push_escape]]）。

**整个路线 4 阶段已闭环上线，可直接在任何新会话用「用 wk-kg 推荐能渲染 XX 的悟空组件」启用**。

**2026-06-11 全组件学习（党建大屏演练暴露问题→完善工具）**：
- 演练发现：KG 仅 6/395 卡导致 Sankey/Tabs/Map2d 全 miss、双环比边界无对照、未收录时返回错误候选不提示
- ✅ 元数据全量收录：`scripts/gen_meta_cards.py` 从大屏 skill `assets/component-library.json` 生成 389 张基础卡（verified=false + probe 字段带 dataConfigId/styleConfigId），6 张深度卡保留。**BasicSankey 桑基图在定制组件/其他**
- ✅ 推荐质量三连修：①keyword_search 打分（verified+3/测试组件×0.3/名称整串+4/**检索文本加组件id**/**中文bigram每个+0.5封顶3阈值1.0**——修"桑基流图≠桑基图"匹配失败）②semantic.recommend/search 改 **keyword 为主 LightRAG 仅补充**（教训：LightRAG pipeline 处理中+常见词组件名从回答文本误匹配，噪音排前）③recommend 返回 warning（空命中/unprobed 候选禁止臆造格式）
- ✅ ingest_kg.py --reset（DELETE /documents 全清重灌，解同名 409）；deploy.sh 健康检查重试 6×3s（uv 启动慢误报）
- ✅ 线上回归 7 查询全过：BasicSankey/Tabs系列/Map2d/IndexStatistic/Swiper11×2/BasicLine 全部正确置顶
- ✅ **深度探测完成（2026-06-11，全在 gczx 跑）**：端点 `GET unity/config/{configId}` → result.data 即官方默认 cardData（不是 card-data/{dataConfigId}）。`probe_schemas.mjs` 自动登录(见 [[reference_wukong_auto_login]]) 拉 **377 cardData + 395 style，0 失败**(内网 38ms/请求~1min)；`backfill_cards.py` 注入 373 张 `cardDataSchema.format=probed-official`(shape骨架+真实example截断800字) + styleKnobs.officialKnobs；18 控制类无 dataConfig 保持 UNPROBED；4 深度卡补 officialExample。探测产物 `kg-probe/`(gitignore 留 gczx)。回填后 kg 拉回本机 git，本机=gczx/main 一致。**BasicSankey 现有官方 schema**(shape keys=['data','link']，节点带 depth)
- **探测链路全在服务器**：脚本 `/opt/wk-kg-mcp/scripts/{wk-login.mjs,probe_schemas.mjs,backfill_cards.py,gen_meta_cards.py}`，assets/component-library.json(503KB)纳入 git；sm-crypto 在 gczx scripts/node_modules；以后重探只需 `ssh gczx 'cd /opt/wk-kg-mcp && node scripts/probe_schemas.mjs && python3 scripts/backfill_cards.py && python3 scripts/ingest_kg.py --reset'`，再 tar kg 回本机 commit
- 悟空登录页无验证码；component-library.json 一级分类：图表62/多数据78/辅助37/主题33/指标14/地图8/交互21/其他11/定制83/测试48

**fork-cd 写入范式**（已存进 index.json writePattern）：GET 源 cardData → POST unity/card-data 建新副本(不带 id) → batch-modify 改 page-card.dataId；PUT 永远不支持；改页面背景 PUT unity/page/{id} 只发 {style}；删组件后 level 重排必须 byName()。

阶段任务 #15(完成)/#16 搭 wk-kg-mcp/#17 gczx 部署联调/#18 并入大屏 skill。关联 [[project_wukong_screen_builder]] [[project_wukong_knowledge_base]] [[reference_dingtalk_mcp_http_direct]] [[knowledge_graph_skill_v0_2]]

**2026-06-16 模板知识库全量探测（chengshidanao 账号）**：
- ✅ 模板中心（SHARETEMPLATE+RELEASED）：142 条，file_source=`template/center/{id}`
- ✅ 我的空间-模板（STANDARDTEMPLATE+RELEASED）：322 条，file_source=`template/space/{groupId}/{id}`
- ⚠️ 项目模板（project字段非空）：48 条全是模板中心子集，409 冲突，知识已在 KG，缺"关联项目"属性
- LightRAG 总量：859 条（395 组件 + 142 center + 322 space），pipeline 处理中
- 脚本：`D:\git\wk-kg-mcp\scripts\template-explore\`（3个脚本 commit 24ec6a5），gczx `/opt/wk-kg-mcp/scripts/template-explore/`
- 关键坑：① unity/page/list 不分页须 ID 去重 ② LightRAG DELETE 是异步，delete+立即reinsert 仍 409 ③ chengshidanao 凭证 `/root/.wukong-cli/credentials-csdn.json`+`token-cache-csdn.json`
- **惨案**：悟空开发3在 11:21 跑 `ingest_kg.py --reset` 清掉全部 464 条模板数据，已重跑恢复
- **根治（commit e1e6833）**：`ingest_kg.py --reset` 现在自动在组件灌入后顺序跑 center+space 模板探测（`--no-templates` 跳过），路径在 `ingest_templates()` 函数；node 在 `/usr/bin/node`，PATH 已设

**2026-06-18 三路新数据源全量落地（commit da4b99d）**：
- ✅ **项目模板**：`explore-project-templates.mjs`，查 `POST /unity/project/page {condition:{type:8,groupId},paging:{pageIndex,pageSize}}`，5 分组 61 已发布，file_source=`template/project-templates/{groupName}/{id}`
- ✅ **设计库**：`explore-design-library.mjs`，`POST /unity/templates/page {released:true}`，10 条已发布，file_source=`template/design-library/{id}`
- ✅ **素材广场**：`explore-materials.mjs`，4 分类（插画库1327+图片库895+地图素材275+动图库207=2704 条），file_source=`material/{kind}/{id}`；**关键坑：API 用 pageIndex（1-based）不是 pageNum，pageNum 不翻页**
- `ingest_kg.py` TEMPLATE_SCRIPTS 扩展到 5 个（--reset 时全部自动跑）
- **LightRAG 实际总量：3822 文档**（REST API 上限 1000，需查 `kv_store_doc_status.json` 获真实数）
  - 素材/插画库: 1327 | 素材/图片库: 895 | 空间模板: 322 | 组件卡: 395 | 素材/地图素材: 275 | 素材/动图库: 207 | 官方文档/其他: 188 | 模板中心: 142 | 项目模板: 61 | 设计库: 10
- **writeBatch 409 检测坑**：LightRAG 409 响应体是 `{"detail":"Document storage already contains '...'}` 不含字符串 '409'，必须同时检测 `'already contains'` 否则误报 fail（已修复）
