---
name: memory-南京-k8s-验收-hpa-流量伸缩
description: [memory] 南京客户华为云 Stack 8.5.1 CCE 集群，需通过"高频接口动态扩缩容（30s 触发、5min 完成、99.9% 灰度、自动回收）"验收，基于 nginx_ingress_controller_requests + custom.metrics.k8s.io
---

<!-- Auto-generated by skills-manager memory module.
     Source: C:\Users\majq1\.agent-memory\project_nanjing_k8s_hpa.md
     Do not edit here — edit the source and run `skills-manager-cli memory sync`. -->

**Memory type**: unspecified

会话名："南京k8s支持"

**验收要求（红色 2/3 条）**：
- 流量激增时 30 秒内触发扩容，5 分钟完成扩展
- 灰度发布成功率 ≥99.9%
- 高频接口自动资源回收，缩容服务不中断
- **必须看 QPS 指标**（不能用 CPU 兜底）

**当前进度（2026-04-30）**：
- ✅ 已部署社区原版 ingress-nginx v1.7.1（独立 namespace `ingress-nginx-metrics`、IngressClass `nginx-metrics`、NodePort 30080）
- ✅ ServiceMonitor 已让 Prometheus 抓到 controller metrics
- ⏳ 待执行：改 usercenter ingress 切到 nginx-metrics + host=usercenter.local（用户授权可改，"实际没用"）
- ⏳ 待执行：adapter-config 加 rule、HPA、Deployment 优雅停机 patch、wrk 压测
- ❌ wizdom-navigator 不能动（生产关键，外网真实流量持续访问 `/eUrbanMIS/view/wizdom-navigator/`）
- ❌ dex 不动（认证组件 + 有 503 残留问题）

**Why**: 验收只演示一个应用（usercenter）的完整闭环已经够，机制能展示就行。

**How to apply**: 后续如果继续这个项目，所有可执行 yaml/脚本/README 都在 `D:\git\ingress-nginx-metrics\`，按里面 README.md 7 步走。生产路径风险已规避（不动 wizdom 和 dex），只有 usercenter 一个 deployment 的 patch 操作有几秒不可用窗口。

**已踩过的大坑（避免重蹈）**：
1. 09:13-09:40 这段切 usercenter ingress class 但没保留生产兼容，导致老 ingress 30000 端口的 usercenter 流量中断 30+ 分钟（外网真实流量受影响）。后续一定要：先建 demo ingress 验证 → 再考虑切原 ingress（如果业务可接受）
2. 私仓 192.200.25.105:5000 里的 ingress-nginx 镜像（v1.0.4 标签）是被裁剪的，缺 per-ingress QPS 指标，导致一开始判断 metrics 链路有问题花了大量时间排查
