---
name: memory-会话默认操作规则
description: [memory] 用户的稳定操作偏好——除删除类操作外自动通过，避免反复打断节奏
---

<!-- Auto-generated by skills-manager memory module.
     Source: C:\Users\majq1\.agent-memory\feedback_session_rules.md
     Do not edit here — edit the source and run `skills-manager-cli memory sync`. -->

**Memory type**: unspecified

**核心规则（2026-04-13 起，2026-04-28 再次确认为通用偏好）**：

1. **除删除类操作外，所有操作自动通过**，无需每步确认；删除（文件/容器/数据/分支/钉钉记录等）仍需明确确认。
2. （v3 项目专属）所有实际运行都在服务器 `demo.egova.com.cn` 上执行，不在本地 D:\opencode 跑。
3. （v3 项目专属）统计系统前端页面使用中文。

Why: 用户在 2026-04-13 v3 会话首次明确，2026-04-28 在另一独立会话（钉钉表格 MCP 启用任务）中再次主动声明 "当前会话注意，除非删除操作，否则所有操作授权都自动通过"。说明这是跨会话的稳定偏好，不只限于 v3 项目。
How to apply: 写文件 / 运维命令 / 调 MCP / git push 等非删除操作，直接执行不必先问；遇到 `rm` / `git branch -D` / `aliyun ... DeleteInstance` / `mcp__*__delete*` / 钉钉 record/task delete 等才提示用户确认。规则 2、3 仍然只在 dingtalk-report-v3 这个项目里生效。
